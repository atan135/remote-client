我需要实现一个内网-外网的连接系统
是一个双端系统，
内网有个nodejs服务，用于和外网主动连通，以及主动执行本机的指令
外网常驻一个nodejs+express的系统，服务端用nodejs+express实现，界面用vue新版实现
业务逻辑如下：
内网启动nodejs服务后，连通外网，并提供断网重连功能
外网提供一个web服务器，用户打开界面后，可以发送请求给服务器，服务器把请求推送给内网nodejs服务,
内网nodejs服务器收到信息后，可以主动执行命令行指令，将结果返回给界面