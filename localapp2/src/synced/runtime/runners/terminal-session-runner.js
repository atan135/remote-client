// Synced from localapp/src/runtime/runners/terminal-session-runner.js
// Source SHA-256: a398af5dbffcfc1370575997b69113b2e06f0564823988e95dfa765a1fe03009
// Do not edit this file directly.
// Run: node localapp2/tools/sync-localapp-files.mjs --write

import pty from "node-pty";

export class TerminalSessionRunner {
  constructor(config = {}) {
    this.config = config;
  }

  spawnSession({ command, args, cwd, env, cols = 120, rows = 30 }) {
    return pty.spawn(command, args, {
      name: "xterm-color",
      cwd,
      env,
      cols,
      rows,
      useConpty: process.platform === "win32" ? this.config.windowsUseConpty : undefined,
      useConptyDll: process.platform === "win32" ? this.config.windowsUseConptyDll : undefined
    });
  }
}
