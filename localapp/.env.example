SERVER_WS_URL=ws://localhost:3100/ws/agent
AGENT_ID=office-pc-01
AGENT_LABEL=Office PC 01
AGENT_APPLICATION_NOTE=
AGENT_SHARED_TOKEN=
HEARTBEAT_INTERVAL_MS=15000
RECONNECT_INTERVAL_MS=5000
COMMAND_TIMEOUT_MS=120000
MAX_BUFFER_BYTES=1048576
# 可选：all / trace / debug / info / warn / error / fatal / mark / off
LOG_LEVEL=info
LOG_DIR=logs
WINDOWS_OUTPUT_ENCODING=cp936
AUTH_PRIVATE_KEY_PATH=./keys/auth_private.pem
AUTH_PRIVATE_KEY_PASSPHRASE=
# 按实际目标服务端切换，例如 ./keys/webserver_sign_public_local.pem
WEBSERVER_SIGN_PUBLIC_KEY_PATH=./keys/webserver_sign_public.pem
DEFAULT_SHELL=powershell.exe
# Windows 可选：true / false；留空时交给 node-pty 按系统自动选择
WINDOWS_USE_CONPTY=
# Windows 可选：true / false；启用 node-pty 自带 conpty.dll，可绕过部分后台进程下的 AttachConsole 清理报错
WINDOWS_USE_CONPTY_DLL=
# 远程打开文件时单次最多返回多少字节，超出部分只预览前面内容
REMOTE_FILE_MAX_BYTES=1048576
MAX_TERMINAL_SESSIONS=4
SESSION_IDLE_TIMEOUT_MS=1800000
SESSION_OUTPUT_LIMIT=1200
TASK_PROFILE_CONFIG_PATH=./config/tool-profiles.json
# 额外探测的 CLI / 终端命令，多个条目用逗号或分号分隔
DISCOVER_TERMINAL_COMMANDS=
COMMON_WORK_DIRS=C:\\workspace,C:\\project\\remote-client,D:\\sandbox
# 预设命令格式：显示名::命令内容，多个条目用 || 分隔
# PRESET_COMMANDS=主机名::hostname||IP 配置::ipconfig /all||当前用户::whoami
PRESET_COMMANDS=
ALLOWED_CWD_ROOTS=
LOCAL_DEBUG_SERVER_ENABLED=false
LOCAL_DEBUG_SERVER_HOST=127.0.0.1
LOCAL_DEBUG_SERVER_PORT=3210
LOCAL_DEBUG_TOKEN=
